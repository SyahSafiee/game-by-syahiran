/// Which pause-menu panel (if any) is currently showing, published by
/// [RpgGame] alongside `dialogue` for the widget tree to render reactively.
enum MenuScreen { closed, main, inventory, quests }
